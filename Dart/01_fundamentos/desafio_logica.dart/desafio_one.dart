// O Conversor de Tempo (Sem if!)
// O Problema: O usuário digita uma quantidade total de minutos (ex: 135) e você deve mostrar quantas horas e minutos isso representa (ex: "2h 15min").

// Regras:
// Use stdin para pegar o valor.
// Não pode usar condicional (if).
// Você precisará usar os operadores ~/ (divisão inteira) e % (resto da divisão).

// Saída esperada:
// Digite os minutos: 135
// Resultado: 2h 15min
